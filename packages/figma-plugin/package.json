{
  "name": "figma-plugin",
  "version": "0.0.0",
  "description": "Enhance Figma's corner smoothing on large rounded corners",
  "source": "src/index.ts",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "lint": "eslint src --ext ts,tsx",
    "build": "parcel build",
    "dev": "parcel watch"
  },
  "dependencies": {
    "figma-squircle": "*"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "eslint-config-base": "*",
    "tsconfig": "*"
  }
}
